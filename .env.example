# No required secrets for the current MemStorage setup.
# Render sets PORT automatically — do not hardcode it.

# Optional: set if you later wire a real Postgres database
# DATABASE_URL=

# Optional: set if you enable OpenAI features
# OPENAI_API_KEY=
